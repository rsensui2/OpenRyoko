# OpenRyoko 2026.9.25 — モデル選択と既定モデルの運用を簡単に

![モデル選択と運用の使い方](https://github.com/rsensui2/OpenRyoko/releases/download/v2026.9.25/model-management-cover.png)

新しいモデルが出るたびに設定一覧や既定モデルを手で直す負担を減らします。導入済みのCodex・Claude CLIからモデルを取得し、WebとSlackで切り替えられるようになりました。

## 今回できるようになったこと

- **モデル一覧の自動取得**：起動時と6時間ごとに確認。画面から手動更新もできます。対応するCLIプロトコルで列挙される新モデルは、Ryokoへの個別追加を待たずに選択できます。
- **既定モデルの追従**：「自動追従」「通知して選ぶ」「固定」をエンジンごとに設定。Codexは節約にLuna、バランスにSol、性能重視にAstraを使います。
- **Slackの設定カード**：`@Ryoko モデル設定`、またはDMで `モデル設定`。既定・方針・通知先・固定解除・切り戻しを操作できます。
- **社員とCronの固定を一覧化**：実際に使うモデルと継承元を確認し、固定設定をまとめて解除できます。
- **社員の設定を正しく反映（#91）**：APIで作成した社員の会話は、社員のエンジンとモデルを使用します。モデル未指定なら利用エンジンの既定を継承します。

既存環境は、追従方針を明示的に選ぶまで現在の設定を維持します。既定モデルを変更しても、既存会話と社員・ジョブの明示的な固定は維持します。一覧の取得と設定操作ではAIの推論ターンを起動しません。

## 使い始める手順

1. Webの「モデル設定」で一覧を更新します。
2. 用途と追従方針を選びます。まず「通知して選ぶ」で候補を確認できます。
3. Slackで操作する場合は、設定画面で管理者Slack IDを保存し、SlackアプリのInteractivityを有効にします。管理者を接続先の `allowFrom` に含めてください。
4. 通知先と、社員・Cronの固定設定を確認します。

READMEは導入から更新まで書き直し、Jevの空気読み、Claude / Codexのフォールバック、AIなしの定期実行、更新時の運用点検も追加しました。添付のPDF・PowerPointで今回の使い方を解説しています。

## 更新と対応範囲

```bash
npm install -g openryoko@2026.9.25
ryoko migrate --auto
ryoko stop
ryoko start
```

通常の既存環境では `ryoko update --restart` も利用できます。Dockerで本体やCLIをイメージへ組み込んでいる場合は、版とローカルtarballを更新して再ビルド・再作成してください。

モデルの一覧取得・既定変更が今回の自動化対象です。CLIやRyoko本体の自動インストールは含みません。CLIの版やアカウントによって一覧が変わり、掲載されたモデルの応答成功を保証するものではありません。取得に失敗した場合は現在の既定を維持します。

Geminiは従来の手動設定です。Workflowノードと外部スクリプトのモデル指定は、今回の社員・Cron一覧の対象外です。料金区分は用途の目安で、利用量や価格の保証ではありません。

## 検証

本体2,408件・Web127件のテスト、型チェック、両パッケージのビルドを実施。実Codex / Claude CLIからの一覧取得と、隔離したゲートウェイのブラウザで追従・手動選択・切り戻し・固定解除を確認しています。Slackカードの権限と操作は自動テストで検証しています。

公開済みの2026.9.24に続く同日の追加リリースとして、次の未使用番号2026.9.25を採番しています。
