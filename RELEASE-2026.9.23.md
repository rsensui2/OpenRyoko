![OpenRyoko — Opus 5.5 標準対応。](https://github.com/rsensui2/OpenRyoko/releases/download/v2026.9.23/openryoko-opus-5-5-banner.png)

**Claude Opus 5.5に標準対応しました。**

新規セットアップのClaude既定モデルを `claude-opus-5-5` に更新しました。ダッシュボードのモデル選択から、設定・社員・CronなどでOpus 5.5を選べます。1Mトークンのコンテキストと `low` / `medium` / `high` / `xhigh` / `max` のeffortに対応しています。

**新料金を費用推計へ反映**

| 種別 | Opus 5.5の標準API料金（100万トークンあたり） |
| --- | ---: |
| 入力 | $4 |
| 出力 | $20 |
| キャッシュ読取 | $0.20 |
| キャッシュ書込・5分 | $5 |
| キャッシュ書込・1時間 | $8 |

入力・出力の単価はOpus 5比で20%、キャッシュ読取は60%安くなっています。[Anthropic公式発表](https://www.anthropic.com/claude-opus-5-5)

対話モードの費用推計はキャッシュ読取・書込も集計し、会話中にモデルを切り替えた場合も各メッセージのモデルに合わせて計算します。Opus 5と5.5のIDの前方一致による誤判定を防ぎ、旧Opusの入力・出力料金も修正しました。

Cronの記録ではキャッシュ読取・書込を区別できないため、Cron費用画面は従来どおり入力・出力分の下限推計です。表示額は標準API料金ベースで、サブスクリプションの実請求額やFast mode等の加算を表すものではありません。

**既存の設定を引き継ぐ**

既存環境で明示指定したモデルとカスタムモデル一覧は保持します。Opus 5・Opus 4.8・`opus` エイリアスも引き続き選べます。更新後、ダッシュボードのClaudeモデルを **Opus 5.5** に変更すると利用できます。社員・Cronに個別指定がある場合は、そのモデル指定も必要に応じて変更してください。

Claude Codeは最新バージョンを使用してください。新規セットアップのeffort既定値は従来の `xhigh` を維持しています。コストと応答時間を抑えたい場合は、Opus 5.5の公式既定値である `medium` も選べます。[モデル仕様](https://platform.claude.com/docs/en/models/opus-5-5/overview)

**検証**

- 本体2,368件・Web119件、合計 **2,487テスト成功**。
- 型チェック・本番ビルド成功。
- Claude Code 2.1.280で `claude-opus-5-5` / `xhigh` の実接続に成功。応答のモデルIDと1Mコンテキストを確認。
- モデル選択、既定値、明示設定の保持、世代別料金、キャッシュの期間別料金、重複記録の除外を回帰テスト。

**更新方法**

通常のnpm導入環境（Node.js 22以降）：

```bash
ryoko update --restart
```

この版を指定して導入する場合：

```bash
npm install -g openryoko@2026.9.23
ryoko migrate --auto
```

移行後、利用している起動方法に合わせてゲートウェイを再起動してください。読み取り専用Docker環境では、導入版とローカルtarballを同じ公開版へ揃えてイメージを再構築・コンテナを再作成し、その後 `ryoko migrate --auto` を実行します。
