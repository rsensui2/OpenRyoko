# OpenRyoko 2026.9.26 — 系列・深さ・代役を選ぶモデル編成

![モデル編成画面](https://github.com/rsensui2/OpenRyoko/releases/download/v2026.9.26/model-management.png)

モデルの版を指定し直す代わりに、Sol・Terra・Opusなどの系列を選べるようになりました。選んだ系列の最新版へ追従し、社員・定期実行・ワークフローごとに使い分けられます。

## 使い方

1. Webの「モデル設定」を開き、主担当の系列カードを選びます。Solを選んだ設定が、新しいAstraに切り替わることはありません。
2. 「考える深さ」で軽く・標準・じっくり・深く・最大から選びます。モデルが対応する値だけを表示します。
3. 「障害時の切替」をONにすると、利用上限・無応答・タイムアウト時にCodexとClaudeが互いの代役になります。Sol ↔ Opus、Terra ↔ Sonnetなどの初期設定は、CodexからClaudeへの切替先と、ClaudeからCodexへの切替先を、それぞれ変更できます。
4. 「タスクごとの編成」で、社員・定期実行・ワークフローに系列と深さを設定します。個別指定をやめる場合は「親設定を継承」を選びます。社員に紐づく定期実行は、その社員の設定を引き継ぎます。

Slackでも `@Ryoko モデル設定` から系列・深さ・代役を操作できます。「タスクの系列・深さを設定」で対象を選んでください。操作には登録済み管理者のSlack IDとInteractivityが必要です。

## 更新後の動作

代役も系列の最新版を使います。モデルIDを固定しているタスクも、障害時は代役への切替対象です。代役に同じ深さがなければ対応する値に調整し、深さを指定できないモデルには、深さの設定を渡しません。各社の性能・料金が同等であることを保証する対応表ではありません。

系列の更新は起動時と6時間ごとに確認します。取得失敗や系列の消失では現在の設定を維持します。対象のモデル・エンジン・深さを別の画面やファイルで変更した場合も上書きせず、追従を停止して表示します。系列を選び直すと再開できます。

既存環境の固定設定を一括で変更する移行はありません。現在の会話のモデルは保持します。リモート社員と動的なワークフロー設定は個別管理です。CLIやRyoko本体の自動インストールは含みません。

## 更新

```bash
npm install -g openryoko@2026.9.26
ryoko migrate --auto
ryoko stop
ryoko start
```

通常の環境は `ryoko update --restart` でも更新できます。Docker環境はイメージを更新して再作成してください。ローカルtarballを組み込む構成では、そのファイルも更新します。詳しい設定例は [モデル管理ガイド](https://github.com/rsensui2/OpenRyoko/blob/main/docs/model-management.md) にあります。

## 検証

本体2,419件・Web128件のテスト、型チェック、ビルドが成功しました。ブラウザで系列選択・深さ・代役・タスク別追従を確認し、固定モデルの障害時切替は両方向の自動テストで検証しています。
