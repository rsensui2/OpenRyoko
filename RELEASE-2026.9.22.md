![OpenRyoko × Jev — 超高速、空気読み。](https://github.com/rsensui2/OpenRyoko/releases/download/v2026.9.22/openryoko-jev-banner.png)

**Jevを利用したSlackの空気読みに対応しました！**

返事・リアクション・沈黙を選ぶ判断を、Jevで高速に。判定用CLIの起動を省き、会話にどう参加するかを素早く判断します。回答の生成や実作業は、その後にClaude CodeやCodexなどの会話エンジンが引き継ぎます。

会話の流れ、自分の名前・役割・スキルを踏まえ、設定に応じて役立てる場面へ自分から参加することもできます。ダッシュボードでJevへの切替、APIキーの登録、参加率の調整まで行えます。

Claude CodeとCodexの双方向切替、更新に合わせた運用点検、Cronの実登録状態の表示も加わり、日々の仕事を任せるための引き継ぎと運用確認も強化しました。

**⚡ Jevで、空気読みのためのCLI起動を省く**

ダッシュボードから、従来のCLI判定・Jevのみ・JevとCLIの併用・比較運転を選べます。APIキーの入力・接続確認も同じ画面で行えます。

「Jevのみ」では、分類のためにClaude CodeやCodexを起動しません。返信すると決まった後の回答生成・実作業は、これまでどおり会話エンジンが担当します。

社内の回帰評価では、Jevの分類APIの中央値は約0.25秒でした。これは返却検証を通過した応答のAPI時間で、回答生成やSlack配送の時間は含みません。

既存環境は従来のCLI判定を維持します。Jevの利用にはTypeSafeのAPIキーが必要で、判定対象の本文・直近の会話などをTypeSafe APIへ送信します。

**💬「自分が役に立てるか」まで考えるSlack対応**

名前で呼ばれたかに加え、会話の流れ、自分に設定された名前・役割・スキルを踏まえて参加を判断します。

たとえば「毎月この表を手作業で集計している」という会話。集計や自動化のスキルがあれば、未解決の困りごとに提案できる場面として参加を検討します。呼ばれていない時の参加率は0〜100%で設定でき、通常の依頼への返答にはこの割合を適用しません。

軽い感謝やお祝いには、判定後すぐ絵文字で反応。回答や作業へ進む時は👀で知らせます。会話への参加や入力待ちなどの状態は保存され、再起動後も引き継がれます。

未依頼の参加率は既定0%。100%は「役立てると判定した対象場面すべて」に参加する設定です。メンション限定などの接続先の制限は維持されます。

**🔄 Claude CodeとCodexで仕事を引き継ぐ**

SlackやWebの通常会話で、Claude Code → Codex、Codex → Claude Codeの両方向に対応しました。

実際の利用上限、空応答、無応答・タイムアウト時に、会話と作業状況を引き継いで代替エンジンへ切り替えます。元の処理が停止したことを確認し、同じターンで両エンジンを往復し続けないようにしています。

ダッシュボードで「切り替える／待機する」を選択できます。代替側にもCLIの導入・認証が必要です。残量の割合による事前停止は追加していません。

**🔧 新しい機能を、普段の運用改善につなげる**

更新通知ジョブから、稼働中の機能とCron・Slack・Workflowの設定を点検できるようになりました。

「スクリプトだけで実行できる定期処理に、毎回AIを起動していないか」「変化のない確認をコードで済ませられないか」など、改善候補を先にコードで抽出します。候補がない回や、同じ状態を点検済みの回にはAIを起動しません。

- **点検・提案（既定）**：改善案を通知します。
- **ローカル修正を許可**：同じ目的・実行時刻・通知先を保ち、等価性を検証できるCron／ローカルスクリプトの改善まで許可します。
- **点検オフ**：更新通知のみを継続します。

手動の読み取り点検は `ryoko maintenance inspect --json` から利用できます。点検機能は、更新の自動インストールやJevの自動有効化を行うものではありません。

**🕒 Cronの「設定した」と「実行登録された」を確認できる**

ファイルを直接編集したジョブがスケジューラに反映されず、一覧だけには表示される問題を修正しました。

- 削除・再作成を検知し、イベントが届かない場合も30秒ごとに照合します。
- 書き込み途中・不正JSON・一時的なファイル消失では、最後の正常な登録を維持します。
- 画面とAPIに「登録済み／未反映／登録エラー」を表示します。
- API更新の受信中に別の編集が入っても、古い一覧で上書きしないようにしました。

全ジョブの登録を解除する時は、有効な空配列 `[]` を保存してください。ファイル削除だけでは既存登録を維持します。過去に逃した実行を遡って実行する機能はありません。[#75の修正](https://github.com/rsensui2/OpenRyoko/pull/87)

**🧭 設定を相談するための標準スキル**

`openryoko-config` を標準同梱しました。Jevの選択、エンジン切替、運用点検などを、稼働版の対応機能に合わせて案内します。

既存環境にも `ryoko migrate` で未導入の標準スキルを追加します。既存のカスタムスキルやリンクは保持します。Workflowなどで既定以外のモデルを指定する際のGPT-5.6 Terraの解決も修正しました。

**検証**

mainの統合状態で、本体2,361件・Web112件、合計**2,473テスト**が成功。型チェック・本番ビルド・GitHub ActionsのCIも通過しています。

**更新方法**

Node.js 22以降の通常のnpm導入環境では、次のコマンドで更新・移行・再起動できます。

```bash
ryoko update --restart
```

このバージョンを指定して導入する場合：

```bash
npm install -g openryoko@2026.9.22
ryoko migrate --auto
```

移行後は、利用している起動方法に合わせてゲートウェイを再起動してください。複数インスタンスを使っている場合は、対象のインスタンスを指定して移行・再起動します。読み取り専用Docker環境では、導入版を指定してイメージを再構築・コンテナを再作成し、その後コンテナ内で `ryoko migrate --auto` を実行します。ローカルtarballを上書き導入する構成では、そのtarballも同じ公開版へ揃えてください。

更新後、ダッシュボードの空気読み設定でJevを選び、TypeSafeのAPIキーを登録・接続確認すると利用できます。

**関連PR**

[設定スキル #83](https://github.com/rsensui2/OpenRyoko/pull/83) · [モデル解決 #84](https://github.com/rsensui2/OpenRyoko/pull/84) · [Jev・会話追跡・双方向切替 #85](https://github.com/rsensui2/OpenRyoko/pull/85) · [運用点検 #86](https://github.com/rsensui2/OpenRyoko/pull/86) · [Cron登録の同期 #87](https://github.com/rsensui2/OpenRyoko/pull/87)
